# Day 03 – Strings in Python

## ✔ What I Learned Today
- How Python stores and handles strings
- String indexing and slicing
- Common string methods like `upper()`, `lower()`, `strip()`, `replace()`
- Basic operations such as concatenation

##  Concepts Covered
- Accessing characters using indexing
- Slicing: `string[start:end]`
- Useful methods:
  - `upper()`
  - `lower()`
  - `title()`
  - `strip()`
  - `replace()`
  - `len()`

## Tasks Completed
1. Store your name in a variable and perform slicing  
2. Convert the string to upper and lower case  
3. Replace a character in the string  
4. Remove spaces from beginning and end  
5. Print the length of the string  

## Code Written
See `main.py` in this folder.

## 📌 Notes
- Strings are immutable in Python.
- Slicing never gives error—it adjusts internally.
